FROM node:22-alpine

WORKDIR /app

# Install dependencies first (same pattern as prod)
COPY package*.json ./
RUN npm ci

# Copy source (will be overridden by volume in compose)
COPY . .

# React dev server
EXPOSE 3000

CMD ["npm", "start"]